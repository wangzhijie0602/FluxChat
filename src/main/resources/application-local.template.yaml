# Commit this template to GitHub.
# Teammates should copy this file to application-local.yaml and replace placeholder values.
# 将此模板提交至 GitHub。
# 团队成员应将此文件复制到 application-local.yaml 并替换占位符数值。

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fluxchat?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: your_db_username
    password: your_db_password